<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp"
    android:background="#F8F8FF">

    <ImageView
        android:id="@+id/spinner_item_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/item_icon_desc"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_spinner_item" />

    <TextView
        android:id="@+id/spinner_item_room"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:hint="@string/spinner_item_hint"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/spinner_item_icon"
        app:layout_constraintStart_toEndOf="@id/spinner_item_icon"
        app:layout_constraintTop_toTopOf="@id/spinner_item_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>