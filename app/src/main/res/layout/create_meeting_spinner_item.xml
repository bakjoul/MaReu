<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#F8F8FF"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <ImageView
        android:id="@+id/input_room_spinner_item_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:contentDescription="@string/item_icon_desc"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_room" />

    <TextView
        android:id="@+id/input_room_spinner_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:hint="@string/spinner_item_hint"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/input_room_spinner_item_icon"
        app:layout_constraintStart_toEndOf="@id/input_room_spinner_item_icon"
        app:layout_constraintTop_toTopOf="@id/input_room_spinner_item_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>